---
import { formatDate } from '../utils/client';

export interface Props {
  date: Date;
  className?: string;
}

const { date, className = 'capitalize text-xs text-accent mb-1' } = Astro.props;
---

<time datetime={date.toISOString()} class={className}>
  {formatDate(date)}
</time>
